spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/jokboer/config-repo
          username: hello.jiajun@qq.com
          password: 05180629MX.
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      expose: "*"